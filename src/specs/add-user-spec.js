import D from '../utils/data';

describe('Add User', () => {

    it('A.C_1. Validation messages - existing Case Number, less than 3 characters, etc.', () => {


        browser.url('/')

        browser.wait(10000)
    });

});

